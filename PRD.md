# AI 短剧创作工厂 PRD

- 版本：V1.0
- 日期：2026-08-26
- 产品阶段：现有 MVP 的产品化升级
- 依据：对现有网页端、服务端、命令行批量生成、提示词与配置模块的代码梳理

## 1. 产品概述

AI 短剧创作工厂面向短剧编剧、策划和内容团队，将“素材拆解—创意开发—集纲—剧本正文”串成标准化创作流程。用户输入小说 IP、对标剧反应记录或视频内容描述，选择题材与创作要求后，系统调用大模型生成结构化的创作中间件或剧本，并允许复制、导出与后续编辑。

### 1.1 现状结论

当前项目已实现一个可运行的单页 Web 工作台与 Flask API，并提供命令行批量生成入口。网页端已有六个创作环节：

1. IP 梗概与人设提取；
2. 对标短剧拆剧分析；
3. 视频内容描述转带时间戳剧本；
4. 灵感创作与仿写；
5. 集纲生成；
6. 剧本正文生成。

服务端以生成类型选择预置提示词，调用 OpenAI 兼容接口返回文本；本地 `prompts/` 支持独立管理提示词；命令行可读取集纲与人设文件，逐集输出正文 TXT；另有 Word 导出能力。

当前系统的核心定位应从“AI 生成页面”升级为“短剧项目创作工作流”。本 PRD 以此为目标定义下一版产品。

### 1.2 目标用户

| 用户 | 核心诉求 | 关键价值 |
|---|---|---|
| 独立短剧编剧 | 快速将灵感或小说整理成可写剧本 | 缩短从素材到初稿的时间 |
| 策划/责编 | 拆解对标剧、校验节奏与钩子 | 形成可复用的创作分析模板 |
| 小型内容工作室 | 多人推进一部剧的多个创作阶段 | 保留版本、可交接、可审核 |

### 1.3 目标与非目标

**业务目标**

- 将单次生成升级为“项目—资产—版本”的可追溯创作过程。
- 让用户可在一个项目中完成从 IP 素材到分集剧本的闭环。
- 通过结构化模板、质量检查和导出降低返工。

**本期不做**

- 不提供视频/音频自动识别；“扒一卡”仅处理用户提供的文字描述或时间轴。
- 不承诺生成结果可直接拍摄，仍需人工审核、改写及版权合规判断。
- 不构建复杂的多人实时协同编辑器；本期只提供基础成员、评论与状态流转。

## 2. 用户流程

```text
新建项目
  → 导入素材 / 录入创作 Brief
  → IP 提炼或对标拆解
  → 确认故事设定与人物小传
  → 生成、编辑、锁定集纲
  → 按集生成剧本正文
  → 质量检查、版本对比、审核
  → 导出 Word / TXT
```

每一步的结果都应作为项目资产保存，并可作为下一步默认输入。用户可以跳过不需要的步骤，但系统须提示跳过后可能缺失的上下文。

## 3. 功能需求

### 3.1 项目与资产管理（P0）

**用户故事**：作为编剧，我希望为每部短剧建立独立项目，并保存每次生成和手工修改的内容，从而随时恢复、交接与复用。

| 编号 | 需求 | 验收标准 |
|---|---|---|
| PRD-PROJ-01 | 新建、重命名、归档项目；填写题材、目标市场、预计集数与简介 | 新项目创建后进入工作台，项目信息可编辑并自动保存 |
| PRD-PROJ-02 | 以资产类型管理 IP 梗概、人物小传、拆剧报告、集纲、分集剧本 | 每个资产可在项目内定位、预览、编辑与复制链接 |
| PRD-PROJ-03 | 自动保存草稿及版本历史 | 编辑停顿后自动保存；任一资产可查看历史版本并恢复 |
| PRD-PROJ-04 | 支持 TXT/Markdown 导入与 Word/TXT/Markdown 导出 | 成功导入后保留原文；导出文件可正常打开且内容完整 |

### 3.2 六步创作工作台（P0）

保留现有六个功能，但改为统一的“输入—配置—生成—编辑—保存为资产”交互。

| 环节 | 输入 | 结构化输出 | 本期要求 |
|---|---|---|---|
| IP 提炼 | 小说文本/文字素材、题材、核心卖点 | 核心梗概、主角动机、成长/升级路线、风险点 | 输出采用固定字段，可一键写入项目资产 |
| 拆剧分析 | 对标剧名称、集数、分集 Reaction、分析维度 | 分集集纲、人物弧线、节奏、爽点、钩子与优化建议 | 用户勾选的分析维度必须传入模型 |
| 扒一卡 | 文本化视频描述、起始集数、集数数量 | 带时间戳的分集剧本 | 必须反映起始集数与集数数量，最多 3 集 |
| 灵感/仿写 | 题材、主线、核心爽点；或对标内容与改写方向 | 剧名、人物小传、大纲、差异化要点 | 仿写须显示版权与原创化提示 |
| 集纲生成 | 人设、核心梗概、总集数、输出范围、卡点要求 | 按集剧情、钩子、强卡点 | 总集数、范围和卡点均应进入生成约束 |
| 剧本正文 | 题材、人物小传、逐集集纲、国内/海外风格、集数范围、风格项 | 标准场次剧本 | 每集独立生成并保存，支持失败重试与单集重生成 |

### 3.3 提示词与模型控制（P0）

1. 管理员可创建、编辑、发布、停用提示词模板，并维护名称、说明、版本、适用环节和变量定义。
2. 每次生成须记录提示词版本、模型、参数、输入摘要、耗时、字数和结果版本。
3. 用户可在不破坏默认模板的前提下填写“本次附加要求”；默认不直接编辑线上模板。
4. 支持选择已配置模型；模型选择受服务端白名单控制，不将服务商密钥暴露到浏览器。

### 3.4 生成质量与编辑（P0）

- 生成前显示必填项、预估范围和敏感内容提醒；生成中可取消；失败时可重试并显示可操作原因。
- 生成结果进入可编辑的富文本/Markdown 编辑区，而不仅是只读文本区。
- 提供“格式检查”：检查集号连续性、场次格式、人物名一致性、是否含结尾钩子、是否满足长度区间。
- 提供“上下文引用”：生成剧本时默认引用已锁定的人设、当前集纲和上一集结尾；用户可以手动调整引用范围。
- 提供“比较版本”：至少可比较当前版本与上一版本的段落差异。

### 3.5 批量生产与审核（P1）

- 用户可选择集数范围创建批量任务，展示队列、每集状态、失败原因和重试入口。
- 批量生成须逐集携带必要上下文，不能仅对每集孤立生成。
- 支持“草稿—待审核—已通过—需修改”四个状态；审核人可留下评论。
- 支持一次导出已通过的剧本集合，并生成目录页。

### 3.6 团队与权限（P1）

- 角色：所有者、编辑、审核者、只读成员。
- 默认项目私有；所有者可邀请成员并撤销访问。
- 记录资产的创建、编辑、审核与导出操作。

## 4. 关键交互与页面

### 4.1 项目首页

- 显示项目卡片、当前阶段、最近编辑时间、待审核数量和批量任务状态。
- 可按题材、状态、成员和更新时间筛选。
- 主操作：新建项目、导入素材、继续创作。

### 4.2 项目工作台

- 左侧为六步流程与资产目录；中间为当前工作区；右侧为生成配置、上下文引用与版本记录。
- 上方固定展示项目名称、当前状态、保存状态、导出与分享入口。
- 每步完成后提供“保存并进入下一步”与“仅保存草稿”。

### 4.3 生成面板

- 输入区：项目资产引用、手工输入、文件导入。
- 配置区：题材、目标集数、风格、模型、附加要求。
- 输出区：流式输出、编辑、格式检查、保存版本、重生成、复制、导出。

## 5. 非功能需求

| 分类 | 要求 |
|---|---|
| 性能 | 单次生成请求应支持流式返回；非模型处理接口 P95 小于 1 秒 |
| 可靠性 | 生成任务具备唯一任务 ID、状态机和可重试机制；超时/限流不可丢失用户输入 |
| 安全 | 密钥仅存于服务端密钥管理或环境变量；日志与接口返回不得包含密钥；生产环境关闭调试模式 |
| 隐私 | 项目内容默认私有；提供删除项目及相关资产的能力；导出行为写入审计日志 |
| 版权合规 | 仿写入口明确提示不得复制受保护表达；输出附带“需人工审核版权与事实”的提示 |
| 可观测性 | 记录请求 ID、任务状态、模型、耗时、失败类型和用量；不记录完整敏感文本到普通日志 |

## 6. 当前实现与缺口

| 维度 | 已具备 | 主要缺口 / 本期处理 |
|---|---|---|
| 创作环节 | 网页端六步入口与多种预置提示词 | 各步输出没有保存为项目资产，也未自动串联上下文 |
| 生成服务 | 统一生成 API、错误返回、耗时/字数回传 | 无流式输出、任务队列、取消、重试、用量与模型白名单 |
| 提示词 | 本地文本文件及基础变量替换 | API 中仍有大量内嵌提示词；无发布、版本、变量校验与审计 |
| 批量创作 | 命令行可按集生成 TXT | 没有 Web 任务管理；上一集内容虽缓存但未传给生成器，剧情衔接未真正实现 |
| 导出 | 可导出 Word，CLI 输出 TXT | Web 端未接入导出；Word 导出缺少章节、标题、样式与目录 |
| 前端交互 | 响应式单页、加载态、复制、示例 | 多个界面参数未接入请求：对标剧名/集数、分析勾选项、集纲总数与范围、剧本国内/海外风格与集数范围/风格项 |
| 安全 | 支持环境变量覆盖配置 | 当前配置文件存在明文密钥，应立即轮换并移出版本库；接口 CORS 与调试模式需按环境收紧 |
| 文档 | 具备启动和 API 说明 | 文档中的 API 前缀与实际实现不完全一致，默认端口描述也不统一，应同步修订 |

## 7. 成功指标

| 指标 | 目标（上线后 8 周） |
|---|---|
| 首次完成率 | ≥ 65% 的新项目完成至少“IP 提炼 → 集纲”两步 |
| 有效产出 | ≥ 45% 的生成结果被保存为项目资产 |
| 创作效率 | 从创建项目到产出首版 10 集集纲的中位时长降低 40% |
| 质量 | 格式检查一次通过率 ≥ 85% |
| 稳定性 | 可恢复的生成失败率 < 3%，非模型接口可用性 ≥ 99.5% |

## 8. 优先级与里程碑

### M0：安全与一致性修复（1 周）

- 立即轮换并移除配置文件中的明文密钥，加入忽略规则与示例配置。
- 统一 API 路由、默认端口与运行文档；生产环境关闭调试模式。
- 补齐前端已展示但未传递的参数，或先移除误导性控件。
- 将提示词从 API 内嵌文本迁移至受版本管理的模板目录。

### M1：单人项目闭环（3–4 周，P0）

- 项目、资产、自动保存、版本历史与三类导出。
- 六步工作台统一输入输出，支持上下文引用与可编辑结果。
- 生成任务状态、流式输出、失败重试和基础质量检查。

### M2：规模化生产（3 周，P1）

- 批量逐集生成、队列、任务监控与跨集上下文。
- 审核状态、评论和批量导出。

### M3：团队协同与优化（2–3 周，P1）

- 成员、角色、审计记录。
- 模型/提示词效果看板与质量指标闭环。

## 9. 验收场景

1. 编剧新建 80 集项目，导入小说文本，生成并保存核心梗概与人物小传。
2. 编剧基于已锁定资产生成 1–20 集集纲，系统在每集末尾输出钩子，并按配置标出强卡点。
3. 编剧选择第 3 集生成正文，系统自动引用人物小传、第 3 集集纲和第 2 集结尾；结果可编辑、通过格式检查、保存为新版本。
4. 编辑创建第 1–10 集批量任务；网络或模型限流导致第 4 集失败时，其他集状态清晰，用户可仅重试第 4 集，且输入不丢失。
5. 审核者将第 1–10 集标记为“已通过”并导出 Word；导出文件包含项目名、剧名、集号和正确的段落层级。
6. 任意普通用户无法读取项目密钥或其他私有项目；日志、接口响应和导出内容不包含密钥。

## 10. 待确认的产品决策

1. 首发市场：仅国内竖屏短剧，还是同时支持海外本地化创作？这决定模板、内容规范与审核规则。
2. 80 集是否为默认规格，还是应按平台/题材提供 30、60、80、100 集预设？
3. 团队协同是否需要对接现有账号体系与云存储？若需要，应在 M1 前确定身份认证和数据存储方案。
4. 是否需接入多模型并进行成本/质量路由？若是，需先定义默认模型、可选模型及预算上限。

---

本 PRD 将现有能力作为 MVP 基线。优先完成 M0 与 M1 后，产品才具备可安全复用、可连续创作的最小生产价值。
